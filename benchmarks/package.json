{
  "name": "cred-verifier-benchmarks",
  "version": "1.0.0",
  "type": "module",
  "description": "ICP performance benchmarks for academic credential verification — research paper metrics",
  "scripts": {
    "bench":             "node run.js --suite all",
    "bench:scalability": "node run.js --suite scalability",
    "bench:concurrency": "node run.js --suite concurrency",
    "bench:finality":    "node run.js --suite finality",
    "bench:dryrun":      "node run.js --suite all --dry-run",
    "visualize":         "python3 visualize/generate_graphs.py"
  },
  "dependencies": {
    "@dfinity/agent":     "^2.1.3",
    "@dfinity/candid":    "^2.1.3",
    "@dfinity/identity":  "^2.1.3",
    "@dfinity/principal": "^2.1.3"
  }
}
